#include<iostream>
using namespace std;

int n, a, b;
int main(){
	freopen("data.txt", "r", stdin);
	cin >> n >> a >> b;
	string s; cin >> s;
	for (int i = 0; i<n; i++){
		if (s[i]!='*'){
			
		}
	}
	return 0;
}