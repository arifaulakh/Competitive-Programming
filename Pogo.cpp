#include<iostream>
#include<cmath>
using namespace std;

int N, M;

int main(){
    scanf("%d%d", &N, &M);
    if (M == 1) printf("%d", 1);
    if (M == 2){
        if (n <= 0) printf("%d", 0);
        if (n ==1) printf("%d", 1);
    }
    return 0;
}