#include <iostream>
#include <cstring>
#include <functional>
#include <queue>
using namespace std;
vector <int> adj;

int main(){
    int N, M: scanf("%d%d", &N,&M);
    for (int i = 1; i <= M; i++) scanf("%d%d%d", &){
        int a, b, w;
        scanf("%d%d%d", &a, &b, &c);
        adj[a] = pair(w, a);
        adj[b] = pair(w, b);
    }
    return 0;
}