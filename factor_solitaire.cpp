#include <iostream>
#include <cmath>
using namespace std;
int N, dp[MAX]; 
const int MAX = 5000001;
int factors[MAX];

int main(){
    factors[0] = 0;
    for (int i = 1; i <= MAX;++i) factors[i]+=1;
    scanf("%d", &N);
    int c = 1;
    

    for 
}