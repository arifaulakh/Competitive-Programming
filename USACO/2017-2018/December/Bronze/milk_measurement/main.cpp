#include<bits/stdc++.h>
using namespace std;
const int MAXN = 101;
int N,bessie[MAXN],elsie[MAXN], mildred[MAXN],cnt = 0;
string winner[MAXN];

int main(){
	// ofstream fout("measurement.out");
 //    ifstream fin("measurement.in");
	freopen("data.txt","r", stdin);
	cin >> N;
	for (int i = 1; i<=N; i++){
		int day, num; string cow; char sign; cin >> day >> cow >> sign >> num;
		if (sign=='-')num*=-1;
		if ()

	}
	return 0;
}