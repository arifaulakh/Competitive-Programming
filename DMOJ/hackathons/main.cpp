#include<bits/stdc++.h>
using namespace std;
const int MAXN = 1e6+6;
int N, Q, t[MAXN], wow[MAXN];

int main(){
    freopen("data.txt","r",stdin);
    cin >> N;
    for (int i = 1; i<=N; i++){
        cin >> t[i] >> wow[i];
    }
    return 0;
}