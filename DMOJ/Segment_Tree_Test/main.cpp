#include<iostream>
#include<cmath>
using namespace std;
typedef long long ll;
const ll MAXN = 100005;
ll N, M, x, y, num[MAXN];

int main(){
	freopen("data.txt", "r", stdin);
	cin.sync_with_stdio(0); cin.tie(0);
	cin >> N >> M;
	for (int i = 1; i<=N; i++){
		cin >> num[i];
	}
	
	return 0;
}