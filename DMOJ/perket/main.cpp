#include<bits/stdc++.h>
using namespace std;
typedef long long ll;
const ll MAXN = 11;
ll N, S[MAXN], B[MAXN], diff;
int main(){
	freopen("data.txt","r",stdin);
	cin >> N;
	for (int i = 1; i<=N; i++){
		cin >> S[i] >> B[i];
	}
	diff = abs(S[1]-B[1]);
	for (int i = 1; i<N; i++){
		
	}
	return 0;
}