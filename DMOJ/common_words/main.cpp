#include<bits/stdc++.h>
using namespace std;
int n;

int main(){
	freopen("data.txt","r",stdin);
	cin >> n;
	for (int i = 0; i<n; i++){
		int m, k;
		cin >> m >> k;
		unordered_map<string, int> x;
		string s[m];
		for (int i = 0; i<m; i++){
			cin >> x[i];
		}
		for (int i = 0; i<m)
	}
	return 0;
}