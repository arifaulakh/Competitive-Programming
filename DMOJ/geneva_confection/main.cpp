#include<bits/stdc++.h>
using namespace std;
const int MAXN = 1e5+5;
int T, N, arr[MAXN], c[MAXN];

int main(){
	freopen("data.txt","r", stdin);
	cin >> T;
	while(T--){
		cin >> N;
		int cnt = 0;
		for (int i = 1; i<=N; i++){
			cin >> arr[i];
			int x = arr[i];
			c[i] = x;
		}
		
	}
	return 0;
}